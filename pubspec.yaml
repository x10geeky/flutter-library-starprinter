name: star_printer
description: This is library star micro printer
version: 0.0.1
author: Programmer Blackwizard
homepage:

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.x10geeky.star_printer
    pluginClass: StarPrinterPlugin

